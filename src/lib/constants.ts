/**
 * Constants for MemCycle algorithms and settings
 */

// SM-2 Algorithm defaults
export const DEFAULT_EASE_FACTOR = 2.5;
export const MIN_EASE_FACTOR = 1.3;

// Leitner System box intervals (in days)
export const BOX_INTERVALS = [1, 3, 7, 14, 30] as const;

// Popup/Reminder defaults
export const DEFAULT_INTERVAL_MINUTES = 25;
export const AUTO_DISMISS_SECONDS = 30;
